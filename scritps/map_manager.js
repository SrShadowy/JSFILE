// write the history in Maps files
// Here you can generencie to maps on game
class Game
{

    // so you can draw an img background and events to make collision
    static set_background(img, context)
    {
        context.drawImage(img, 0,0);
    }
    
}